import Cell from "./cell";
const Year = () => {
  const placeholder = "Y";
  return (
    <div>
      <Cell placeholder={placeholder} />
      <Cell placeholder={placeholder} />
      <Cell placeholder={placeholder} />
      <Cell placeholder={placeholder} />
    </div>
  );
};

export default Year;
